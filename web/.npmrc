# Lockert npm-peer-dep-Resolution. React/Next-Ecosystem hat oft enge
# Peer-Constraints die in der Praxis funktionieren - mit dieser Einstellung
# verhindern wir spurious Build-Fehler wie ESLint 8 vs 9.
# Aequivalent zu --legacy-peer-deps Flag bei jedem npm-Aufruf.
legacy-peer-deps=true
